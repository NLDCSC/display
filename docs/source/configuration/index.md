# Configuration

The main config file for display is located at `display/webapp/config` and sets reasonable defaults for most of 
the variables. They can be controlled/overwritten by a `.env` file or by manually setting environment variables.

The configuration settings for display can be divided in the following categories:

```{toctree}
:glob:
:maxdepth: 1

contents/*
```
